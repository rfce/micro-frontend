{
  "name": "products",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/products/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project products --web",
  "targets": {
    "build-prod": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "pnpm nx build products",
          "pnpm nx preview products"
        ],
        "parallel": false
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/products",
        "commands": [
          "vite build --mode development", 
          "concurrently \"vite build --watch --mode development\" \"vite preview\""
        ],
        "parallel": false
      }
    }
  }
}